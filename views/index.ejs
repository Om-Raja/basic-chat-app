<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat app</title>
</head>
<body>
    <h1>Chats</h1>
    <hr>
    <% for(chat of chats) { %>
        <div class="chat">
            <h5><%=chat.from%>&rarr;<%=chat.to%></h5>
            <div class="message"><%= chat.message%></div>
            <sub><%=chat.createdAt%></sub>
        </div>
    <% } %>
</body>
</html>